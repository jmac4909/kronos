# Kronos Terminal-First Completion Audit

Audit date: 2026-07-15

## Outcome

The repository is ready for the operator-owned human feedback pass in `HUMAN_FEEDBACK_CHECKLIST.md`. The automated suite proves the product boundary and the main synthetic journeys. It does not replace a live VS Code test with the operator's Claude, Jira, GitLab, Jenkins, and SonarQube configuration.

## Requirement Evidence

| Area | Automated evidence | Status |
| --- | --- | --- |
| Product surface | Manifest gate enforces exactly Work, Sessions, Projects, Attention, 40 commands, and 10 settings. The activation harness registers and directly exercises every command. Runtime graph rejects cycles and dead runtime exports. | Pass |
| Jira board | Unit, activation, DOM, and board tests cover all composed subsets of search, status, Jira-namespace, explicit-local-project, and label filters; completed-work defaults; reset/rerender behavior; registered projects; branches; explicit Add/Change/Unlink linkage; exact Enter/Space card activation; current/empty/loading/partial/stale/error/no-match states; retained last-known tickets; and bounded actions. | Pass |
| Jira context | Unit tests cover bounded REST reads, newest-comment fetching with chronological rendering, recursive empty-value pruning, meaningful `false`/`0`, arbitrary raw attachments, byte/count limits, hashes, and partial warnings. | Pass |
| Terminal ownership | Unit and activation tests prove explicit validated Claude launch, one launch per action, no automatic Jira insertion, exact managed-attachment verification before and after evidence fetch, exactly-once inert `sendText(..., false)` context placement, and no transcript reads or terminal disposal. | Pass |
| Sessions | Unit and activation tests cover project-centered labels, multiple explicit Jira contexts per session, context insertion into any explicitly managed terminal, focus/reconnect, pause/resume, detach, stop management, removal, audit persistence, launch races, and terminal-close races. Terminal objects remain open. | Pass |
| Projects and Git | Tests cover the dedicated Projects view, configurable bounded discovery, registered-first selection, authoritative uncheck, canonical real-path identity, duplicate-path collapse, editable display labels that do not rewrite links/configuration, integration setup, explicit-only ticket links across repositories sharing one Jira namespace, branch reads without Git execution, on-demand read-only VS Code Git loading, unavailable/clean/staged/modified/untracked/conflicted status, caps of 500 paths and 512 KiB, redacted `[GIT-project]` context, and linked launch directories. | Pass |
| Merge requests | Tests cover current-branch discovery, ticket fallback, ambiguity refusal, durable local bindings, initial healthy/mergeable attention, partial review reads, known-MR opening, and prefilled new-MR browser navigation without creating an MR. | Pass |
| Jenkins and SonarQube | Tests cover bounded Jenkins/Sonar reads, retained Jenkins build targets, deterministic latest-first choices with saved timestamps, Sonar branch dashboard links, persisted monitored-branch choices, literal `sonar.projectKey` discovery from Jenkins XML, and rejection of expression-only values. | Pass |
| Attention | A focused source-checked matrix covers every declared GitLab MR, GitLab pipeline/job/test, Jenkins, and SonarQube transition kind. Pure presentation tests cover all six severities, visible project/provider/subject/observed/changed/why fields, ticket-scoped actions, validated latest-first branch/build choices, clear/snooze/audit language, and a durable local setup blocker that stays quiet until recovery or regression. Additional tests cover initial and cleared-but-still-open MR reminders, editable insertion, and restart-safe newest-state projection without stale-row resurrection. | Pass |
| Setup and Doctor | Unit, DOM, and activation tests cover one shared readiness snapshot from first run through fully ready, blocked-first ordering, one bounded per-row action, private comment-only provider template creation, missing versus invalid configuration, current live authentication/permission/reachability/optional-evidence diagnostics, stream-specific recovery, immediate Poll Now verification, credential-presence labels, and no credential values in UI. Guided Settings returns to Setup; both panels show the same native private-state/provider-file paths and precise reload/environment-precedence guidance. | Pass |
| Security and dependencies | Public-surface, security, and context-governance gates pass. Runtime dependencies are empty; only TypeScript and Node/VS Code type packages are development dependencies. `npm audit` reports zero known vulnerabilities. | Pass |
| Packaging | `npm run package` starts with the public-surface scan and the complete manifest/runtime/security/context/roadmap/claim gates, compiles, then compares `vsce ls` against the exact intended runtime/assets/documents before creating the VSIX. Unexpected, missing, sensitive, generated, source-map, development, or dependency files fail closed. `npm run feedback:ready` adds the safe synthetic feedback state, and `npm run publish:verify` checks the clean local branch against its configured remote after push. | Pass; post-push verifier required after each publish slice |
| Quality evidence | `docs/verification-matrix.json` retains the broad feature summary and maps fingerprinted individual roadmap requirements to named tests, repository checks, or explicit human gates. `scripts/check-roadmap-evidence.js` inventories all 230 G01-G22 statements/cases/completion clauses and fails stale mappings closed. Every G01-G22 clause now has direct automated or explicit human evidence; broad real-terminal, real-VS-Code, Windows, live-provider, and multi-window outcomes remain human-gated where synthetic evidence cannot substitute for operator signoff. The inventory now reports 178 automated, 52 human-gated, and 0 open requirements. | Evidence map complete; recorded human gates remain required |
| State and provider contracts | `docs/state-ownership.md` gives every persisted/in-memory record one writer, ingress, bound, compatibility rule, and consumer. `docs/provider-contract-matrix.md` records request, bound, normalization, completeness, and error behavior for all four providers; the quality gate checks both documents' required rows. | Pass |
| Scale and accessibility | `docs/scale-accessibility-budget.md` records shipped collection/read/poll and local timing budgets. A dedicated gate builds 500 ticket cards, renders 200 project forms and 200 maximally rich Session summaries, summarizes 2,000 supplied audit events to 500, bounds an oversized provider preview, checks labels/focus/forced-colors/narrow-panel invariants, and points to newer-refresh supersession and poll-coalescing regressions. Real VS Code screen-reader, zoom, and paint behavior remain human gates. | Automated pass; human gate open |
| Orchestration boundaries | The exact public command inventory now has one pure responsibility router that must match the manifest and activation harness. Work refresh concurrency, deterministic transition recording, provider-read health normalization, provider binding reconciliation, provider environment/readiness, current Attention projection, and project catalog/identity/configuration behavior have focused owners/tests outside activation. The transition matrix fails when a declared provider transition kind lacks a case; the runtime graph rejects cycles and dead exports. Activation and polling retain only cross-workflow sequencing and VS Code/operator notification decisions. | Pass |
| Provider health visibility | Session records persist last attempt, last fully successful poll, last meaningful transition, current normalized error, and quiet unchanged-poll count. Sessions display their health directly; Projects derive one aggregate plus the next scheduled poll from the configured interval. No health-only update creates Attention history. | Automated pass; real provider/view gate open |
| Context Basket | Focused store, reference-parser, message, HTML, and DOM tests cover private bounded selections, exact-artifact deduplication, same-source conflicts, provenance/freshness/completeness/size/hash display, explicit refresh, retained source artifacts after remove/clear, reference-only immutable bundles, and non-submitting insertion through the existing exact-terminal placement boundary. | Automated pass; real terminal/view gate open |
| Local evidence search | A focused 2,000-entry budget gate covers session titles, explicit Jira contexts, project names/branches, provider bindings, artifact labels, and event summaries; every visible field is normalized and bounded, result actions retain only their local target, and terminal objects/content are structurally excluded from index input. The index is ephemeral and rebuilt from private canonical state on each open. | Automated pass; real Quick Pick gate open |
| Local handoff and branch profiles | Focused tests cover capped context/audit candidate selection, credential redaction, immutable private Markdown/JSON pairs, reference/hash-only export, source-payload and terminal-content exclusion, external-path/selection-limit refusal, 20-profile parsing/formatting, unsafe/duplicate/credential validation, exact MR-branch routing, explicit fallback, and unchanged ticket-project identity. | Automated pass; real project UI/live routing gate open |

## Changes Found During This Audit

- The evidence gate now derives 230 stable fingerprinted requirements from every G01-G22 goal statement, case, and completion clause, reports direct automated/human/open counts per goal, and rejects stale test, checklist, repository-check, or changed-roadmap references instead of treating broad goal presence as acceptance proof.
- G01 now has direct evidence for each canonical identity boundary: Jira namespaces never imply repositories, explicit links remain ticket-specific, unlinked tickets remain project/provider-free across lifecycle operations, standalone sessions accept zero/one/several explicit Jira contexts, provider subjects remain resource-specific, and legacy identities migrate or fail closed.
- G02 now has a focused terminal-lifecycle suite plus activation regressions for workspace/home launch, linked/unlinked tickets, attaching an existing terminal without launching or writing, duplicate-name identity and chooser behavior, lifecycle states, fetch/focus/close races, and removal isolation. Real VS Code focus/reconnect on Linux and Windows stays open for operator signoff.
- G03 now has direct evidence for reviewable immutable Jira/MR/CI/Git artifacts, explicit complete/partial/mixed/unavailable/truncated CI states, raw byte-preserving attachments, editable-focus keyboard behavior, duplicate-initialization refusal, exact attachment revalidation, and exactly-once non-submitting insertion. Real terminal and PowerShell behavior stays open for operator signoff.
- G04 now has one documented Attention stream identity across baselines, transitions, acknowledgements, open-MR reminders, and projection. Direct sequences prove newest-only failure/recovery/partial state, occurrence replacement across pipelines/builds/tests/gates/issues, complete append-only reconstruction, and deterministic module-reload/lease recovery; live-provider and multi-window behavior stays open for operator signoff.
- G05 now has one provider-binding reconciliation owner across Work, Sessions, Projects, Attention, polling, and context insertion. Durable MR identity wins stale catalog state, matching monitor state may only enrich that identity, ambiguous discovery never guesses, CI resources stay distinct, multi-project sessions cannot cross, and browser URLs are credential-free and pinned to the configured origin; live-provider behavior stays open for operator signoff.
- G06 now routes every Windows/POSIX open-flag decision through the shared private-file primitive and has focused recovery coverage for active/expired leases, replacement identities, malformed/partial/oversized/future/symbolic state, legacy migration, and isolated session cleanup. Runtime disposal immediately releases an in-flight lease and prevents its late provider read from persisting; Windows-native and two-window behavior stay open for operator signoff.
- G07 now reports provider read, artifact write, normalized snapshot, terminal insertion, session update, and audit append as separate outcomes. Session and audit persistence are attempted independently after a successful non-submitting insertion, partial Jira/Jenkins and mixed CI reads retain their valid components, missing configuration is distinct from another-window lease ownership, and current failure state coexists with last-known-good timestamps. Live-provider behavior stays open for operator signoff.
- G08 now keeps Jira namespace and explicit local repository terminology separate from builder types through DOM state, composes every filter subset, restores/reset filters deterministically, exposes prominent Add/Change/Unlink controls, routes every declared action once, and bounds dense/sparse cards with accessible focus and names. Real VS Code accessibility and the two-obvious-action terminal path stay open for operator signoff.
- G09 now keeps Projects as a peer repository inventory, canonicalizes discovery aliases, skips symbolic child traversal, assigns unique stable identities to duplicate names, retains checked missing folders, confirms authoritative unlinking, distinguishes every bounded Git status, opens existing or prefilled MR pages without provider mutation, blocks project context until an explicit managed target exists, and projects readiness without provider identifiers or credentials. Real VS Code folder-picker, tree, and exact-terminal behavior stays open for operator signoff.
- G10 now makes every Session row an Open Terminal bookmark, renders project/title, observed branch, explicit Jira contexts, terminal lifecycle, monitoring state, and latest result consistently for ticket and standalone project sessions, and keeps reconnect, detach, pause, resume, poll, audit, stop, and confirmed removal semantically distinct. Real VS Code focus/reconnect remains open for operator signoff.
- G11 now gives Attention one pure current-row presentation and action contract. Rows show project/provider/subject/severity/observed/changed/why, provider setup blockers transition once and recover in the same stream, validated Jenkins/Sonar targets sort latest-first, only explicit Jira contexts unlock ticket/MR/CI actions, clear language distinguishes open-MR snooze from ordinary transition clearing, and audit history remains directly available. Real VS Code readability and live-provider noticeability remain open for operator signoff.
- G12 now makes Setup the single guided configuration surface while raw VS Code settings are explicitly advanced. Setup and Doctor share one readiness list and identical runtime path/reload guidance, cover first/partial/ready/invalid/permission/reachability/optional-evidence states, give every blocker one bounded action, and keep credentials presence-only. Project registration still opens provider mapping immediately; saved values can be reviewed or cleared, and observed Git branches prefill new monitoring defaults. Real VS Code clarity and native Windows behavior remain open for operator signoff.
- G13 now closes the bounded enterprise provider contract for Jira, GitLab, Jenkins, and SonarQube. Sanitized fixtures and focused request tests cover rich Jira work and attachments, encoded GitLab subgroup/numeric identities plus partial pagination, Jenkins multibranch/optional APIs/literal Sonar discovery/TLS scope, Sonar branch gate/measures/issues/dashboard routing, unknown extra fields, and credential-free GET-only provider reads. GitLab partial evidence now retains a safe permission or rate-limit cause. Live-provider version/permission/endpoint compatibility remains open for operator signoff.
- G14 now gives the 40-command surface one pure, manifest-matched responsibility router; missing callbacks fail registration closed and explicit Claude routes remain security-audited. Work refresh, Session/Project/Attention presentation, provider binding/transition reconciliation, and shared staged failures remain in focused owners. Dependency/registration coverage moved from the monolithic unit script into a dedicated suite while one `npm test` gate still runs it, and the source graph enforces reachability, acyclicity, and no dead runtime exports.
- G15 now has one focused cross-record invariant suite anchored to the ownership matrix. It proves canonical Work, Session, binding, artifact, monitoring, event, and setup shapes; explicit optional/partial values; normalized dates, branches, URLs, paths, and hashes; unknown-field stripping; migration-only aliases; and fail-closed future schemas. Persisted Session and audit artifact references are re-confined to `KRONOS_DIR` on every read, and current MR consumers no longer probe compatibility branch names.
- G17 now measures the full configured Work/Project/Session/audit scale plus an intentionally oversized provider preview. The composer bounds all rendered preview fields and leaves the payload behind its private artifact link; Session tooltips and ticket workspaces summarize large context/binding collections. The checked budget covers every collection and points to cancellation, explicit-refresh supersession, and poll coalescing regressions. Real keyboard traversal, screen-reader speech, high contrast, zoom, and narrow-panel paint behavior remains open for operator signoff.
- G18 now makes the release surface executable evidence rather than an ignore-file assumption. The preflight derives every expected `out/*.js` file from `src/*.ts`, combines it with an exact asset/document list, compares that to `vsce ls`, rejects runtime dependencies and sensitive/development paths, checks documentation links and package metadata, and runs the public scan before either tests or packaging. A separate post-push verifier refuses dirty, detached, missing-upstream, or local/remote-mismatch states.
- Repeated provider-read failures create durable transitions only when the normalized source state or error changes.
- Every declared GitLab MR, GitLab pipeline/job/test, Jenkins, and SonarQube transition kind now has a source-checked table case, and the current Attention projection has a pure owner that rebuilds deterministically from the durable audit after restart.
- Jira, MR, CI, and Git composers now capture one exact session/binding/terminal attachment before fetch and re-resolve it before placement. Detachment or rebinding during fetch cancels the stale composer, a failed terminal send can be retried, and any successful send permanently consumes that composer even if its later local audit update fails or another queued message arrives.
- Setup, Doctor, Projects, and project integration now consume one secret-free provider configuration model. Setup and Doctor render the same canonical snapshot, every row has one bounded action, the private environment editor creates only a private comment-only template when absent, and Poll Now remains an explicit read-only operator action.
- Provider-read health, progress failures, Jira refresh, project setup, provider context warnings, and common panel actions now share a bounded redacted error vocabulary with one safe next action. Authentication and permission are distinct, as are timeout, DNS, TLS, redirect, rate limit, missing target, response bound, malformed response, pagination, lease contention, local state, network, and unknown availability.
- Operator-visible command workflows and managed-provider polling logs now use that bounded redacted vocabulary exclusively; a source guard prevents raw arbitrary error messages from returning to either boundary.
- Project Git status/diff warnings and Work, Sessions, Projects, and Attention tree refresh fallbacks now use the same bounded redacted failure vocabulary; a credential-shaped Git failure regression proves status remains usable without leaking the raw error.
- Provider-environment loads, Work-catalog issues, work-session record issues, and TerminalFirstState load/watch fallbacks now use that same vocabulary before their details can reach Setup, Doctor, Work, logs, or persisted diagnostic state.
- Eight project registration, identity, provider-configuration, discovery, and Git-pointer tests now run in a dedicated `project:catalog` suite instead of the monolithic activation/unit harness; `npm test` remains the single gate and quality metrics count both owners.
- Six provider-environment parsing/safety, shared-readiness, and Setup/Doctor projection tests now run in a dedicated `provider:readiness` suite; the quality gate counts that suite while keeping the total test metric derived.
- The Jira board DOM gate now exercises all 32 subsets of its five composable filters, every individual mismatch, and exact Enter/Space card activation so filter and keyboard claims cannot rest on markup alone.
- Projects Git-status presentation now has a pure owner and focused cases for unavailable, clean, staged, modified, untracked, conflicted, and over-limit evidence instead of keeping untested status math inside the tree renderer.
- Setup and Doctor now project current Jira refresh and per-provider audit health over static configuration readiness, so a configured provider cannot appear ready after a live authentication, permission, reachability, or partial-read failure; a later complete read clears only its own stream.
- The monitoring lease, durable work-session records, and MR, pipeline, read-health, and combined CI snapshots now share one private-file foundation: Windows omits unsupported `O_NOFOLLOW` and compensates with path/descriptor identity verification; POSIX requires the kernel flag; writes are bounded, complete, private, and same-directory atomic. Each work-session record has an explicit 4 MiB read/write cap and rejected records appear as Doctor issues.
- Sessions, polling readiness, and command guards now share one lifecycle projection. Reloaded attachments become detached until explicitly reconnected, a VS Code terminal exit is recorded as closed history, explicit detach stays detached, paused monitoring stays separate, and stopping management never claims the still-open operator terminal was closed.
- GitLab polling, status, and context insertion now share one target resolver: the newest valid durable binding owns MR identity, matching provider evidence may enrich it, configured local-project identity supplies its read target, and stale catalog MR identity cannot override it.
- Work catalog schema v2 stores one explicit `linked_local_project`; schema-v1 links migrate, legacy inferred project tags are discarded, unavailable links are cleared with a load issue, and future schemas fail closed.
- Work filtering now keeps Jira namespaces and explicit local projects in separate typed facets, selectors, persisted board state, and card attributes so one identity cannot satisfy the other filter.
- Work now projects one Jira refresh lifecycle into both the tree and full board. Deferred reads show loading over retained rows; partial reads report retained counts and warnings; stale data is timed from the configured interval; failures keep last-known tickets visible; and empty or filter-no-match states remain distinct.
- Attention now shows only the newest transition for each project/provider/facet. Recoveries, later failures, builds, pipelines, and gate results replace stale rows; acknowledging the newest row cannot resurrect an older one. The append-only audit history is unchanged.
- Monitoring and Attention now share a canonical project-or-session/provider/resource/logical-subject/facet stream key. Separate MRs and SonarQube branches stay independent; pipeline IDs and Jenkins build numbers are occurrences whose newer state replaces their stale row; GitLab read health stays scoped to its MR.
- Clearing an open MR now snoozes it until the next successful GitLab poll. That poll records one fresh reminder; unchanged polls do not duplicate an uncleared reminder, and merged or closed MRs stay cleared.
- Jenkins job configuration and observed build targets are stored separately, so multiple real builds remain available from Attention.
- Jenkins build choices are deterministically latest-first even when bindings share a timestamp.
- Jenkins XML discovery skips expression-valued Sonar settings while still accepting a later safe literal setting.
- Direct command coverage now exercises project Git/MR actions and the session pause, resume, detach, stop, and remove workflow.
- The activation harness now exercises all 40 contributed commands, including project display-label rename, private local handoff creation, local evidence search, opening the Context Basket, adding another Jira context to a managed terminal, synthetic successful Jira refresh, editable MR/CI context insertion, project-routed provider context, and manual provider polling without using a live endpoint.
- The safe feedback state now includes validated paused/detached ticket and standalone Sessions plus synthetic MR, Jenkins, SonarQube, and repeated-failure Attention evidence. This supports visual review without launching a terminal or contacting a provider.
- The public-surface gate now rejects local-state paths, machine-specific home paths, known employer identifiers, private-key material, and high-confidence token shapes before the remaining test suite runs.
- VS Code types are pinned to the advertised 1.85 minimum. Optional terminal shell-integration metadata is read through a compatibility shim so newer editors retain the extra CWD detail without making that later API mandatory.
- Recruiter-facing product renders use only synthetic `DEMO-*` records, generic project names, and example-only provider state; their editable SVG sources are excluded from the packaged VSIX.
- The board render shows an achievable unfiltered state, while the context-composer render labels its composer and terminal views as consecutive interaction steps rather than simultaneous UI.
- Packaged README links are pinned to the intended public `main` branch so image and documentation URLs remain stable after release.

## Operator-Only Signoff Still Required

Run `npm run feedback:ready`, install the produced VSIX, and complete `HUMAN_FEEDBACK_CHECKLIST.md`. The human pass must confirm:

- actual terminal focus, title, Claude startup, typing, and non-submitting insertion in the target VS Code build;
- visual clarity and keyboard behavior of the Jira board, composer, Setup, and Doctor;
- live provider authentication, pagination, permission behavior, and organization-specific response shapes;
- Windows filesystem and terminal behavior on a real Windows extension host;
- no provider, repository, or terminal mutation during the live journey.

## Improvement Roadmap

The prioritized cleanup, hardening, edge-case, and next-feature goals are maintained in [extension-improvement-goals.md](extension-improvement-goals.md). That roadmap distinguishes current automated evidence from required Windows, live-provider, and operator-owned terminal signoff.
